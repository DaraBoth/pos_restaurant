{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the DineOS app",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "shell:default",
    "process:default",
    "updater:default"
  ]
}
